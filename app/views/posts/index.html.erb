<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<%= link_to 'New Post', new_post_path %>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content/Body</th>
      <th>User</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= image_tag post.image.url(:thumb) %></td>
        <td><%= post.user.email %></td>
        <td><%= link_to 'Show', post %></td>
        <% if post.user === current_user %>
          <td><%= link_to 'Edit', edit_post_path(post) %></td>
          <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>



        
            <li class="category">Social Media Links</li>
            <li><a href="https://www.facebook.com/visitdubai.US/?brand_redir=517762121588320">Facebook</li>
            <li><a href="https://www.youtube.com/visitdubai">Youtube</a></li>
            <li><a href="https://www.instagram.com/visit.dubai/">Instagram</a></li>
            <li><a href="https://twitter.com/visitdubai">Twitter</a></li>
      


